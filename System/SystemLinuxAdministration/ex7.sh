find / -name "*.c" 2>/dev/null

:'
searches the entire system ("/") for files with the ".c" extension and redirects any error messages to "/dev/null" to avoid cluttering the output.

FNG (File Name Globbing) alone does not provide the capability to perform recursive searching throughout the entire system. 
'
